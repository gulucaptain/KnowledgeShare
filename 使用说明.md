# Gulucaptain 使用说明

Gulucaptain 使用 Hugo 生成静态网页，可部署到 GitHub Pages 或 Netlify，不需要数据库和后端服务器。

## 查看与更新

双击根目录的 `index.html` 查看本地预览。修改内容后，双击「更新网页.command」重新生成页面。

## 内容结构

网站采用“主题目录 + 独立资料”的结构：

```text
content/post/
├── wam/
│   ├── _index.md
│   ├── openwam.md
│   └── fastwam.md
├── world-model/
│   ├── _index.md
│   └── duet-dino.md
└── audio-video-generation/
    ├── _index.md
    └── dreamx-creator.md
```

- `_index.md` 定义主题名称、简介和更新时间。
- 同一目录下的其他 Markdown 文件是该主题的资料条目。
- 主题页面和首页更新动态按条目的 `lastmod` 倒序排列，最近更新排在最前面。
- 每页默认显示 5 条；在 `hugo.toml` 修改 `topicPageSize` 可以统一调整。
- Hugo 自动生成上一页、下一页和页码，不需要手工维护分页文件。

## 主题语义索引

`data/topics.yaml` 是主题索引，记录每个主题的：

- 固定名称与目录标识；
- 主题语义范围；
- 典型研究问题；
- 中英文关键词。

新增资料前，先根据标题、摘要、方法与应用场景和该索引比较。核心研究问题与现有主题一致时加入已有目录；只有核心任务明显不同、现有主题无法准确覆盖时才建立新主题。

例如，DUET-DINO 预测未来视觉表征以支持潜空间规划，因此属于 `World Model`；OpenWAM 统一研究世界建模与动作生成，因此属于 `WAM`。

## 新增资料

在对应主题目录中新建 Markdown 文件：

```markdown
---
title: "模型或项目简称"
paper_title: "论文完整标题"
date: 2026-09-12T20:00:00+08:00
lastmod: 2026-09-12T20:00:00+08:00
summary: "一句话概括这条资料。"
tags: ["主要主题", "辅助标签"]
website: "https://example.com/"
project_url: "https://example.com/"
paper_url: "https://arxiv.org/abs/0000.00000"
code_url: "https://github.com/example/project"
model_url: "https://huggingface.co/example/project"
thumbnail: "images/<主题目录>/图片文件.png"
thumbnail_alt: "图片内容说明"
thumbnail_caption: "官方概览图"
thumbnail_source: "原图地址"
draft: false
---

这里写一小段介绍。

{{< website title="模型或项目简称" url="https://example.com/" >}}
```

论文条目使用 `paper_title` 保存完整论文标题，它会成为主题页中该子模块的标题；`title` 保存较短的模型或项目名，并显示在录入信息中。普通网页资料可以省略 `paper_title`，此时直接使用 `title` 作为标题。

来源链接使用以下字段，页面会自动生成带图标的 GitHub 风格按钮，并且只显示已经填写的项目：

- `project_url`：Project Page；
- `paper_url`：arXiv；
- `code_url`：Code；
- `model_url`：Model & Data。

`website` 用于网页预览卡片；通常与 `project_url` 相同。没有某类官方链接时直接省略对应字段，不要填写空链接，也不需要在正文中重复书写链接。

新增后，将所属目录 `_index.md` 的 `lastmod` 更新为同一时间。首页会按主题的更新时间排序，主题页面及首页更新动态按每条资料的 `lastmod` 排序。

如果官方页面提供合适的概览图，将图片保存到 `static/images/<主题目录>/`，再填写上述四个 `thumbnail` 字段。主题页面会自动采用左侧缩略图、右侧文字的排列；手机端自动改为上下排列。

`date` 和 `lastmod` 必须精确到小时和分钟。页面会将录入时间显示为 `YYYY-MM-DD HH:MM`。更新已有资料时保留 `date`，更新条目与主题的 `lastmod`；主题页同时显示“录入于”和“更新于”。首页动态与日历按每条内容最近更新的时间统计，不是历次编辑日志。

## 新建主题

只有语义索引中没有合适主题时才新建：

1. 在 `data/topics.yaml` 添加主题名称、说明、语义范围和关键词。
2. 创建 `content/post/<主题目录>/_index.md`。
3. 在该目录添加第一条资料。
4. 重新生成网站并检查主题索引、分页和搜索。

## 搜索

顶部搜索框覆盖资料标题、摘要、正文和标签。搜索索引直接嵌入静态 HTML，因此本地双击 `index.html` 和在线部署都可以使用。

## 部署

- GitHub Pages 使用 `.github/workflows/pages.yml` 自动构建并发布 `public/`。
- 仓库 Settings → Pages → Build and deployment → Source 必须选择 **GitHub Actions**，不要选择从 `main` 的根目录发布。根目录 `index.html` 是本地预览入口，会跳转到 `.preview/index.html`；将它发布到线上会导致首页打不开。
- 发布后须确认同次提交没有另一套分支发布覆盖 Hugo 产物，并实际打开线上首页和更新的主题页。仅看到部署成功或 HTTP 200 不足以确认网页正常。
- Netlify 使用根目录的 `netlify.toml`，自动安装 Hugo 0.166.0、生成 `public/`，并采用当前部署域名。

`public/` 是发布构建，`.preview/` 是本地预览；两者均由构建过程自动生成。
