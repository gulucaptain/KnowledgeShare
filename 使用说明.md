# ElephantFlow

使用当前目录中下载的 [MeiK2333/github-style](https://github.com/MeiK2333/github-style) Hugo 主题，保留其原有博客样式。上游 README.md 与 LICENSE 原样保留。

## 查看

双击根目录 `index.html`。这是本地预览入口，不需要后台服务。

修改文章后双击「更新网页.command」重新生成。当前电脑的 Hugo 已放在 `.runtime/`；换电脑时安装 Hugo 0.166.0 和 Python 3.9+ 即可继续本地生成。GitHub 自动发布不需要本地 Python。

## 添加或更新记录

在 `content/post/` 新建 Markdown 文件：

```markdown
---
title: 记录标题
date: 2026-09-12T09:00:00+08:00
lastmod: 2026-09-12T09:00:00+08:00
summary: 一句话概括这条记录。
tags: ["主题一", "主题二"]
draft: false
---

这里写来源链接、摘录和自己的理解。
```

- `tags` 就是主题，一条记录可属于多个主题。
- 修改旧记录时更新 `lastmod`；记录列表按最近更新时间排列。
- 原生更新日历仍使用文章发布日期 `date`。
- 点击「更新记录」上方或文章卡片上的主题标签，即可筛选该主题。
- `draft: true` 不展示；未来日期的文章默认不会提前发布。

`content/readme.md` 是首页简介；`hugo.toml` 配置网站名、个人简介、头像与 GitHub 地址。当前使用主题自带 GitHub 图标作为头像，没有使用上游作者的个人头像。

## GitHub Pages

工作流位于 `.github/workflows/pages.yml`，已改成 Hugo 构建，只有 `public/` 会发布。

1. 将代码推送到 GitHub 仓库。
2. 在 Settings → Pages 中将 Source 设为 GitHub Actions。
3. 运行 Deploy Hugo blog，或提交到 main 触发发布。

尚未执行远程推送或部署。默认配置地址为 `https://gulucaptain.github.io/KnowledgeShare/`，实际构建使用 GitHub Pages 提供的地址。

## 本次修改范围

直接使用下载的布局与 CSS；增加站点配置、首页简介和起始记录。将原生导航文案改为中文，记录列表按 lastmod 排列，修复项目子路径下的图片链接和当前 Hugo 兼容问题。关闭评论及搜索，不增加编辑器、登录、上传或额外管理功能。

`public/` 是发布构建，`.preview/` 是可双击打开的本地构建，均可重新生成。旧 `.halo/` 和 `.runtime/` 隐藏目录未删除，也不会发布。

## 记录网站并显示小窗预览

例如「Embodied AI + WAM，https://openwam-official.github.io/」对应：

```markdown
---
title: "OpenWAM"
date: 2026-09-12T17:35:00+08:00
lastmod: 2026-09-12T17:35:00+08:00
summary: "OpenWAM 项目网站。"
tags: ["Embodied AI", "WAM"]
website: "https://openwam-official.github.io/"
draft: false
---

[OpenWAM 项目网站](https://openwam-official.github.io/)
```

将此文件放在 `content/post/`。`tags` 填主题，`website` 填 HTTPS 网站地址，文章详情会自动显示可展开的小窗和「在新窗口打开」按钮。首页与更新记录列表保持原有卡片样式，点击记录标题进入预览。

`website` 不填就仍是普通文章。外部网站若禁止嵌入，预览会受到限制，但原网站链接始终可用。修改后运行「更新网页.command」更新本地页面；提交到 GitHub 后才会更新线上网站。

### 当前记录规则：一个主题，一个项目

WAM 的全部短记录统一维护在 `content/post/wam.md`。首页与更新列表只有一个「WAM」项目，FastWAM、OpenWAM 是项目内的小节，不再分别建文章。

以后收到 `WAM: 网站链接`，直接向该文件追加一个小节，并更新文件顶部 `lastmod`：

```markdown
## 条目名称

[项目网站](https://example.com/) · 记录于 2026-09-12

{{< website title="条目名称" url="https://example.com/" >}}
```

每个小节的预览独立折叠，默认只占一行。WAM 项目保留 Embodied AI 标签，方便从更大主题找到它；小节仍标明原记录各自的主题。

原 OpenWAM、FastWAM 文章链接会转到合并后的 WAM 项目。
